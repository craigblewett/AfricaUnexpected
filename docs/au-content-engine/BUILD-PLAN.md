# AU Content Engine — moved

This work now lives in its own repository:

**https://github.com/craigblewett/au-videos**

| What | Where |
|---|---|
| The plan, stack rationale and deltas to the PRD | `docs/BUILD-PLAN.md` |
| Zod schemas and the grounding validator | `packages/schemas` |
| The spike CLI — facts, ingest, shots, concepts, render | `services/worker` |
| Agent conventions and hard rules | `CLAUDE.md` |

This repository (`AfricaUnexpected`) is a Strapi 5 CMS last touched in May 2025, and is
superseded by `craigblewett/africa-unexpected` — React + Vite on Supabase with a custom
block editor. Neither hosts the Content Engine.

The plan was drafted on this branch only because it was the branch the original session
was scoped to. Nothing here is current; read the new repository instead.
